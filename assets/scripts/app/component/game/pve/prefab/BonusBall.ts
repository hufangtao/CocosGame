const {ccclass, property} = cc._decorator;

@ccclass
export default class BonusBall extends cc.Component {

    // @property(cc.Label)
    // label: cc.Label = null;

    // x;
    // y;
    // time = 0;

    // speedX = 0;
    // speedY = 0;

    // onLoad () {
    //     this.node.setPosition(0,0);
    //     // this.speedX = 10;
    //     this.speedY = 10;
    // }

    // start () {

    // }

    // moveTo(x,y){
    //     this.x = x;
    //     this.y = y;

    // }

    // update (dt) {
    //     this.time += dt;
    //     this.speedX = 10 + 5 - 10 * this.time * this.time;
    // }
}
